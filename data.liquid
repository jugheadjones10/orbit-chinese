---
permalink: data.json
---
[
{% for row in words.macroMetaRows %}
	{
		"word":"{{row.word}}",
		"efactor":"{{row.efactor}}"
		"interval":"{{row.interval}}"
		"repetition":"{{row.repetition}}"
		"dueDate":"{{row.dueDate}}"
		"type":"{{row.type}}"
	}{% unless forloop.last%},{% endunless %}	
{% endfor %}
]

